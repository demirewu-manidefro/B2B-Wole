{
  "name": "ethiopia-b2b-escrow-marketplace-root",
  "version": "1.0.0",
  "private": true,
  "description": "B2B Wholesale & Escrow Marketplace tailored for the Ethiopian commerce ecosystem with dynamic JSONB structural types, pessimistic locking, and real-time negotiation.",
  "scripts": {
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "test": "npm test --prefix backend",
    "test:backend": "npm test --prefix backend"
  }
}
